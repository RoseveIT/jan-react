import React from "react";
import {useForm} from "react-hook-form";

const Form = () => {
    useForm({mode:'all', resolver:})
    return (
        <div>
            
        </div>
    );
};

export {Form};